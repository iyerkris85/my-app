/**
 * Header Styles
 * Style for Header
 */
import { StyleSheet } from 'react-native';

const HeaderStyles = StyleSheet.create({
  centerText: {
    color: '#fff',
    flex: 1,
    fontSize: 20,
    fontWeight: 'bold',
    padding: 10,
  },
});

export default HeaderStyles;
