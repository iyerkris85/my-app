/**
 * Header Styles
 * Style for Header
 */
import { StyleSheet } from 'react-native';

const HeaderStyles = StyleSheet.create({
  headerText: {
    color: '#fff',
    flex: 1,
  },
});

export default HeaderStyles;
